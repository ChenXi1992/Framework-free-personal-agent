# Architecture Guide — superseded

This document is **out of date** and has been replaced.

The current, code-verified architecture reference lives in **`readme_program.md`**
(the canonical program reference for both engineers and AI agents). It covers the
end-to-end request flow, folder/file map, data model, the tool system, a per-module
function reference, and the invariants you must not break.

> Note: `readme_program.md` is gitignored (local-only working reference), so it may
> not be present in a fresh clone. If it's missing, regenerate it from the codebase.

For the user-facing overview, see `README.md`.
